parameters:
- name: receivers
  type: array
  default: 
  - ${{ ci.actor }}

gates:
- name: gate-2
  rule:     
  - qualityTestt.caseCnt_succ <= 6
  - qualityTestt.pass_rate <= 6.7683
  notify-on-fail:
  - type: email
    receivers: ${{ parameters.receivers }}
